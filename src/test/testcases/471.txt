
                var a,b: integer; c: boolean; d: string;
                procedure main();
                var c: real; d: array[-1 .. 3] of real;
                begin
                    c := 1.5 * proc(1.5,2.2);
                end
                function proc(x,y:real):real;
                begin
                    putIntLn(15);
                end
                